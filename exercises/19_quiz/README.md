# Quiz

One file, several C23 features. The `static_assert`s and `assert`s name
the types and values. Fill in the blanks.

If a quiz assertion is confusing, go back to the matching topic
directory — the READMEs are short.
